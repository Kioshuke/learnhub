// Thêm rule này vào Firestore Security Rules:
// =========================
// Cấu hình bảo trì
// =========================
match /settings/{docId} {
  allow read: if true;
  allow create, update, delete: if isAdmin();
}

// Đặt rule này TRƯỚC catch-all rule:
// match /{document=**} {
//   allow read, write: if false;
// }
